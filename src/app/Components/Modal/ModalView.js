import React from 'react'

export const ModalView = (props) => {
	return (
	   <div id={`modal${this.props.id}`} className="modal">
		   <div className="modal-content">
			   <input type={modalContent.inputType} value={value} onChange={handleChange} />
			   <div className="buttons">
				   <span className="helper-text" data-error="wrong" data-success="right">Add {modalContent.text}</span>
				   <button className="right" onClick={handleSubmit}>Add</button>
			   </div>
		   </div>
	   </div>
	)

}